{
  "name": "qa-engineer-take-home",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test tests/dynamic-skip.spec.js",
    "test:api": "playwright test tests/sync-api.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
